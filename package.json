{
  "name": "sf6-combo-weaver",
  "version": "1.0.0",
  "private": true,
  "description": "SF6コンボ確認動画ツール",
  "scripts": {
    "build": "shx rm -rf dist && shx mkdir -p dist && shx cp index.html manifest.json icon.svg dist/ && shx cp -r data dist/ && esbuild index.tsx --bundle --outdir=dist --loader:.ts=tsx --define:process.env.NODE_ENV='\"production\"' --minify"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.23.0",
    "shx": "^0.3.4",
    "typescript": "^5.5.3"
  }
}