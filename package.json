{
  "name": "sf6-combo-weaver",
  "version": "1.0.0",
  "private": true,
  "description": "SF6コンボ確認動画ツール",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -r data dist/data && cp manifest.json dist/manifest.json && cp index.css dist/index.css && esbuild index.tsx --bundle --outfile=dist/index.js --loader:.ts=tsx --define:process.env.NODE_ENV='\"production\"' --minify"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  }
}